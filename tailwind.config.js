/**
 * HyprEarn — Tailwind CSS v4 uses CSS-first configuration (`src/index.css`, `src/styles/design-tokens.css`).
 * This file is kept for tooling that expects a config path; extend here only if you migrate off `@tailwindcss/vite`.
 */
export default {}
